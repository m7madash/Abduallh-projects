export declare class AegisClaw {
    private kernel;
    private agentId;
    private gateUrl;
    private licenseKey;
    constructor(options?: {
        agentId?: string;
        gateUrl?: string;
        licenseKey?: string;
        witnessCallback?: (action: any) => Promise<boolean>;
        heartbeatMs?: number;
    });
    check(prompt: string): Promise<{
        pass: boolean;
        decision: 'PASS' | 'BLOCK' | 'QUARANTINE';
        reason: string;
        coherenceScore: number;
        hash: string;
    }>;
    getSnapshot(): {
        ledger: import("./kernel").Action[];
        capsule: import("./kernel").StateCapsule | null;
        drift: number;
    };
    getMetrics(): import("./kernel").KernelMetrics & {
        security: import("./secure-kernel").SecurityMetrics;
    };
    getHealth(): import("./kernel").HealthStatus;
    getAudit(): any;
    private defaultWitness;
}
export declare function createAegisMiddleware(options: {
    gateUrl?: string;
    licenseKey?: string;
    agentId?: string;
    onBlock?: (reason: string) => void;
    onPass?: (hash: string) => void;
}): Promise<{
    gate(prompt: string): Promise<boolean>;
    instance: AegisClaw;
}>;
