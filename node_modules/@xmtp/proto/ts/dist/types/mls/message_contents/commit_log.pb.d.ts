import Long from "long";
import _m0 from "protobufjs/minimal";
import { RecoverableEd25519Signature } from "../../identity/associations/signature.pb";
export declare const protobufPackage = "xmtp.mls.message_contents";
/**
 * Defines entries on the commit log, used for fork detection and recovery
 * XIP: https://community.xmtp.org/t/xip-68-draft-automated-fork-recovery/951
 */
export declare enum CommitResult {
    COMMIT_RESULT_UNSPECIFIED = 0,
    COMMIT_RESULT_APPLIED = 1,
    COMMIT_RESULT_WRONG_EPOCH = 2,
    COMMIT_RESULT_UNDECRYPTABLE = 3,
    COMMIT_RESULT_INVALID = 4,
    UNRECOGNIZED = -1
}
export declare function commitResultFromJSON(object: any): CommitResult;
export declare function commitResultToJSON(object: CommitResult): string;
/**
 * PlaintextCommitLogEntry indicates whether a commit was successful or not,
 * when applied on top of the indicated `last_epoch_authenticator`.
 */
export interface PlaintextCommitLogEntry {
    /** The group_id of the group that the commit belongs to. */
    groupId: Uint8Array;
    /** The sequence ID of the commit payload being validated. */
    commitSequenceId: Long;
    /** The encryption state before the commit was applied. */
    lastEpochAuthenticator: Uint8Array;
    /** Indicates whether the commit was successful, or why it failed. */
    commitResult: CommitResult;
    /** The epoch number after the commit was applied, if successful. */
    appliedEpochNumber: Long;
    /** The encryption state after the commit was applied, if successful. */
    appliedEpochAuthenticator: Uint8Array;
}
export interface CommitLogEntry {
    sequenceId: Long;
    serializedCommitLogEntry: Uint8Array;
    signature: RecoverableEd25519Signature | undefined;
}
export declare const PlaintextCommitLogEntry: {
    encode(message: PlaintextCommitLogEntry, writer?: _m0.Writer): _m0.Writer;
    decode(input: _m0.Reader | Uint8Array, length?: number): PlaintextCommitLogEntry;
    fromJSON(object: any): PlaintextCommitLogEntry;
    toJSON(message: PlaintextCommitLogEntry): unknown;
    create<I extends Exact<DeepPartial<PlaintextCommitLogEntry>, I>>(base?: I): PlaintextCommitLogEntry;
    fromPartial<I extends Exact<DeepPartial<PlaintextCommitLogEntry>, I>>(object: I): PlaintextCommitLogEntry;
};
export declare const CommitLogEntry: {
    encode(message: CommitLogEntry, writer?: _m0.Writer): _m0.Writer;
    decode(input: _m0.Reader | Uint8Array, length?: number): CommitLogEntry;
    fromJSON(object: any): CommitLogEntry;
    toJSON(message: CommitLogEntry): unknown;
    create<I extends Exact<DeepPartial<CommitLogEntry>, I>>(base?: I): CommitLogEntry;
    fromPartial<I extends Exact<DeepPartial<CommitLogEntry>, I>>(object: I): CommitLogEntry;
};
type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;
export type DeepPartial<T> = T extends Builtin ? T : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>> : T extends {} ? {
    [K in keyof T]?: DeepPartial<T[K]>;
} : Partial<T>;
type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P : P & {
    [K in keyof P]: Exact<P[K], I[K]>;
} & {
    [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
};
export {};
